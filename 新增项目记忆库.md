# 新增项目记忆库 — 操作模板

> 使用者：Carrey | 我爱 Mitra's Song
>
> **给 AI Agent 的操作手册。新增项目时按下面 5 个步骤执行，不放心的先看附录。**
>
> 本文档只教你“新增一个项目”。Docker 运维、环境安装等见主手册 `tencentAgentMemory.md`。

---

## 触发前：收集参数

你必须从用户那里拿到 **全部三个参数**。缺任何一个，不要往下走，直接问用户要。

| 参数 | 示例 | 怎么获取 |
|------|------|---------|
| `API_KEY` | `sk-031c0c287a9a...` | 用户给你 DeepSeek 的 key |
| `TEAM_ID` | `team-p9jfw5wz6u` | 用户在 Panel 里点进 Team，URL 里就有 |
| `AGENT_ID` | `agt-p9mimzhm61` | 用户在 Panel 里点进 Agent，URL 里就有 |

> 如果用户还没有 Team / Agent：告诉他去 http://localhost:8125 用 `sk-mem-l5V1Gpjq4J8Cy2ucItLXP9aFYWmEMZsK` 登录创建，再把 ID 给你。

---

## 步骤 1：复制配置文件

```powershell
cd E:\carrey\tencent-agent-memory\TencentDB-Agent-Memory\MemoryProxy
Copy-Item config.yaml config-{项目名}.yaml
```

**不要改 `config.yaml`**。只改新文件 `config-{项目名}.yaml`。

## 步骤 2：修改新配置文件

只改这三个字段，其他全部不动：

```powershell
cd E:\carrey\tencent-agent-memory\TencentDB-Agent-Memory\MemoryProxy

# 替换 apiKey
(Get-Content config-{项目名}.yaml -Raw) -replace 'apiKey: .*', "apiKey: `"${API_KEY}`"" | Set-Content config-{项目名}.yaml

# 替换 team_id
(Get-Content config-{项目名}.yaml -Raw) -replace 'team_id: .*', "team_id: `${TEAM_ID}`" | Set-Content config-{项目名}.yaml

# 替换 agent_id
(Get-Content config-{项目名}.yaml -Raw) -replace 'agent_id: .*', "agent_id: `${AGENT_ID}`" | Set-Content config-{项目名}.yaml
```

> PowerShell 命令中的 `` `" `` 是转义双引号，照着抄就行。

**验证替换结果**：

```powershell
Select-String -Path config-{项目名}.yaml -Pattern 'apiKey|team_id|agent_id'
```

确认输出中三个值都变成了用户给你的真实值，并且各自只出现了一次。如果 `apiKey` 出现了 2 次以上，说明误伤了其他字段，报告用户。

## 步骤 3：启动 Proxy

端口 8096 只能跑一个 Proxy。先检查有没有旧的在跑：

```powershell
netstat -ano | findstr ":8096.*LISTENING"
```

**有输出** → 记下最后一列的 PID，停掉它：`taskkill /PID <PID> /F`

**无输出** → 直接启动：

```powershell
cd E:\carrey\tencent-agent-memory\TencentDB-Agent-Memory\MemoryProxy
npm run dev -- --config config-{项目名}.yaml
```

启动成功的标志：看到 `server.listening {"address":"0.0.0.0","port":8096}`。

> 如果启动报错，停止操作，把完整错误信息发给用户。

## 步骤 4：验证链路

按顺序执行 4 个子步骤。**任一步失败就停止，向用户报告失败在哪一步。**

### 4a. 确认 Proxy 能转发

```powershell
node -e "fetch('http://localhost:8096/v1/messages',{method:'POST',headers:{'Content-Type':'application/json','x-api-key':'sk-local','x-conversation-id':'verify-'+Date.now()+'-'+Math.random().toString(36).slice(2)},body:JSON.stringify({model:'deepseek-v4-pro',max_tokens:20,messages:[{role:'user',content:'回复 OK'}]})}).then(r=>{console.log('HTTP',r.status);return r.text()}).then(t=>console.log(t.slice(0,200)))"
```

> `x-conversation-id` 里带了时间戳和随机数，确保和之前的测试不重复（避免 Core 去重）。

期望输出：`HTTP 200` 且内容包含 AI 回复。如果 502 或超时，停止并向用户报告。

### 4b. 去 Panel 确认记忆写入

打开 http://localhost:8125 → 用 `sk-mem-l5V1Gpjq4J8Cy2ucItLXP9aFYWmEMZsK` 登录 → 进入用户的 Team → 点「Memory」。

应该能看到刚才 `verify-xxx` 会话的对话记录。

> **看不到**：告诉用户「验证未通过：Panel 无记录」，然后执行下面 4c 做 API 级确认。

### 4c. API 级核验（Panel 查不到时用）

```powershell
node -e "
fetch('http://localhost:8420/v3/conversation/query', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer local',
    'x-tdai-service-id': 'default',
    'x-tdai-agent-id': '{AGENT_ID}',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ limit: 5 })
})
.then(r => r.json())
.then(d => console.log('Total:', d.data?.total ?? JSON.stringify(d).slice(0, 500)))
.catch(e => console.error('Error:', e.message))
"
```

注意替换 `{AGENT_ID}` 为用户的真实值。`Total` > 0 说明链路正常只是 Panel 没刷新。

> 这一步失败（Total=0），立即停止，报告用户「Core 查询无数据，可能链路异常」。

## 步骤 5：在新项目中启动 Claude Code

验证全部通过后，用脚本启动：

```powershell
$scriptPath = "E:\carrey\tencent-agent-memory\TencentDB-Agent-Memory\claude-with-memory.ps1"
if (-not (Test-Path $scriptPath)) {
    Write-Error "claude-with-memory.ps1 不存在，请检查路径: $scriptPath"
    exit 1
}
& $scriptPath -ProjectDir "{新项目的绝对路径}"
```

脚本自动检测 Proxy 在线状态：在线走记忆，离线自动回退直连。

> **不要直接敲 `claude`**。直接敲会走 `settings.json` 的 DeepSeek 直连，对话不进记忆库。

---

## 当前项目清单

| 项目 | 配置文件 | Team ID | Agent ID |
|------|----------|---------|----------|
| TencentDB-Agent-Memory | `config.yaml` | `team-p9jfw5wz6u` | `agt-p9mimzhm61` |
| _（项目名占位，Agent 执行后替换）_ | `config-{项目名}.yaml` | `{TEAM_ID}` | `{AGENT_ID}` |

---

## 附录 A：安全底线（出问题时再看）

遇到以下情况，**立即停止，不要自己动手修**，向用户报告：

| 你遇到了什么 | 你要干嘛 |
|-------------|---------|
| Proxy 启动报错 | 报完整错误，等用户决策 |
| 验证 4a/4b/4c 任一步失败 | 报失败在哪一步，等用户决策 |
| 用户没给你全部 3 个参数 | 列出缺什么，等用户提供 |
| 任何涉及 Docker 的需求 | 说你不能操作，引导看主手册 |
| 任何涉及修改 `config.yaml` 的需求 | 报要改什么，等用户确认 |

**绝对不碰的东西**：Docker 容器（stop/start/rm）、`config.yaml`、`MemoryProxy/src/` 源码、系统持久化环境变量（`SETX` / `[Environment]::SetEnvironmentVariable`）。Shell 级临时变量（`$env:XXX = "..."`）是安全的，不受此限。

## 附录 B：踩坑速查（排查问题时看）

| 现象 | 原因 | 怎么做 |
|------|------|--------|
| Panel 无记录但 Proxy 日志显示 `write-l0` | `write-l0` 在发 HTTP 请求前就打了，不等于写入成功。postForCtx 失败时静默返回 `{}` | 用 4c API 核验，不依赖 Proxy 日志 |
| Claude Code 走了直连，记忆没进库 | `settings.json` 的 env 优先级高于系统变量 / 忘了设 Shell 变量 | 改用 `claude-with-memory.ps1` 脚本启动 |
| Core 查询返回 `accepted_ids:[]` | 同一 session_id 的重复消息被去重了 | 每次测试换新 `x-conversation-id`（带时间戳+随机数） |
| Panel 团队列表空白 | Hub 的 `REMOTE_INSTANCE_ID` 和 Core 的 serviceId 不一致 | 告诉用户看主手册的容器恢复章节 |
| Panel 登录失败 | admin key 被重置或容器被重建 | 同上，引导看主手册 |
| 对话中用了 `/memory`，但 Panel 没记录 | `/memory` 是 Claude Code 本地功能，不经过 Proxy | 正常对话即可，TDAI 自动提取 |
