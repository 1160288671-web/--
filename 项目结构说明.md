# 定制旅行方案产品 — 项目结构说明

> 一个「定制化旅行方案」产品：以一套可安装的 `custom-travel-plan` skill 为方法论，落地为一个分轮对话的 Web 应用 `travel-planner-app`，从需求收集到产出可执行旅行方案。

---

## 目录结构一览

```
travel/                                   # 仓库根
├── MEMORY.md                             # 项目记忆索引（指向各关键文档）
├── 项目结构说明.md                        # 本文件
├── custom-travel-plan.skill              # skill 打包副本（zip：SKILL.md + references/）
├── custom-travel-plan skill 设计方案.md   # skill 设计说明：目标 / 流程 / 取舍 / 维护
├── 定制化旅行方案讨论记录v1.md            # 方法论讨论稿（思考过程与依据）
│
├── .claude/                              # AI 协作配置
│   └── CLAUDE.md                         # 协作契约：演进链路、目录约定、开发约定
│
├── .bin/                                 # 本地脚本（沙箱用）
│   └── npm                               # bash 包装：转发 npm.cmd "$@"
│
└── travel-planner-app/                   # Web 应用（React 19 + TS + Vite）
    ├── index.html                        # Web 应用入口
    ├── vite.config.ts                    # Vite 配置：端口 3000、@ 别名、注册 travelApiPlugin
    ├── package.json                      # 依赖与脚本（dev/build/lint/preview）
    ├── tailwind.config.js                # Tailwind 主题（shadcn 配置）
    ├── postcss.config.js                 # PostCSS 处理
    ├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
    ├── eslint.config.js / components.json
    ├── .env.example                      # 环境变量模板（LLM_API_KEY / LLM_BASE_URL / LLM_MODEL / AMAP_KEY / AMAP_SECRET）
    ├── .env                              # 本地配置（含真实 Key，已 gitignore）
    ├── .gitignore
    ├── README.md                         # 应用说明：快速开始、环境变量、目录结构
    ├── template-info.md                  # 脚手架生成记录（shadcn 主题与组件清单）
    ├── start.ps1                         # 一键启动：装依赖 → 检查 .env → npm run dev
    │
    ├── server/                           # 服务端（Vite 插件内嵌 API，无独立后端进程）
    │   ├── travel-api.ts                 # 插件入口：装配 /api/config、/api/chat、/api/fetch-url、/api/plan-map
    │   ├── prompts/
    │   │   └── system-prompt.ts          # SYSTEM_PROMPT：角色、分轮规则、输出 JSON 协议、可信度原则
    │   └── lib/
    │       ├── llm.ts                    # resolveLlmConfig / chatCompletion（OpenAI 兼容客户端）
    │       ├── url-fetch.ts              # 网页正文抓取 + 纯文本提取（extractText / fetchPageText）
    │       ├── amap.ts                   # 高德地理编码 + 静态图点位标注拼装（fetchPlanMap）
    │       └── http.ts                   # readBody / sendJson 小工具
    │
    └── src/                              # 前端
        ├── main.tsx                      # 入口：挂载 React（StrictMode + BrowserRouter）
        ├── App.tsx                       # 路由定义（/ → Home）
        ├── index.css                     # 全局样式
        ├── App.css                       # 应用样式
        │
        ├── pages/
        │   └── Home.tsx                  # 主页面：对话区 + 轮次进度 + 快捷选项 + 输入区
        │
        ├── components/
        │   ├── travel/
        │   │   └── ProfilePanel.tsx      # 需求画像面板（实时汇总 profile，硬约束 ⚠ 标记）
        │   └── ui/                       # shadcn/ui 组件库（40+，自动生成，勿手改）
        │       ├── button.tsx / card.tsx / textarea.tsx / scroll-area.tsx ...
        │
        ├── lib/
        │   ├── api.ts                    # fetchConfig / sendChat / fetchUrlText / parseModelReply
        │   └── utils.ts                  # cn()（clsx + tailwind-merge）
        │
        ├── hooks/
        │   └── use-mobile.ts             # 响应式断点 hook
        │
        └── types/
            └── index.ts                  # ModelReply / ChatTurn / ApiMessage / PROFILE_KEYS / ROUND_LABELS
```

---

## 仓库三件套

仓库根的三份文件是同一方法论的三个演进阶段，改动必须沿此链路同步：

| 文件 | 角色 | 改什么 |
|------|------|--------|
| `定制化旅行方案讨论记录v1.md` | **讨论稿**：思考过程、依据、全景清单 | 新想法先沉淀到这里 |
| `custom-travel-plan skill 设计方案.md` | **设计文档**：目标、分轮流程、三层分析法、取舍记录 | 流程/架构层面的决策 |
| `custom-travel-plan.skill` | **skill 实现**：打包副本（SKILL.md + references/question-bank.md） | 实际执行的问题措辞与话术 |

**skill 内容**（解包 `custom-travel-plan.skill` 可见）：

```
custom-travel-plan/
├── SKILL.md                      # 主流程：原则、分轮工作流、三层分析法、产出要求
└── references/
    └── question-bank.md          # 问题库：各轮问题模板、选项设计、条件分支、话术
```

- **SKILL.md 精简**（约 100 行），只装「流程怎么走」，每次触发都加载，控制上下文成本
- **question-bank.md** 体量大，放到 references 按需加载
- 两份文件职责互斥：流程逻辑只在 SKILL.md，具体问题措辞只在 question-bank.md

---

## 各组件详解

### 1. 核心方法论：分轮迭代 + 三层分析

**分轮结构**（skill 与应用 SYSTEM_PROMPT 共同遵守）：

```
Round 0  快速定位    → 判断客户类型（目标明确型 / 模糊意向型 / 纯探索型），决定从哪轮进
Round 1  骨架信息    → 出发地 / 时间 / 预算 / 同行人 / 目的地意向
Round 2  动机与偏好  → 旅行动机 / 参照样本 / 玩法倾向 / 出片意愿
Round 3  细节与底线  → 吃 / 住 / 行 / 节奏 / 雷区 / 证件健康
Round N  按需迭代    → 补缺口、解矛盾；上限 4 轮纯提问
```

**三层分析法**（顺序不可颠倒）：

```
硬约束 → 可行域       （预算/日期/证件/健康/雷区，做减法）
   ↓
需求翻译 → 真实诉求    （"想看海" = 放松?玩水?拍照? 用假设反问确认）
   ↓
软偏好加权 → 排序取舍  （冲突时把取舍摆给客户选，不替他决定）
```

### 2. travel-planner-app — 应用整体

| 层 | 位置 | 职责 |
|----|------|------|
| 前端 | `src/` | 对话 UI、轮次进度、需求画像、方案展示 |
| API | `server/travel-api.ts` | 装配 3 个接口，转发 LLM 调用 |
| Prompt | `server/prompts/system-prompt.ts` | 旅行定制师角色与全部行为规则 |
| 工具 | `server/lib/` | LLM 客户端、链接抓取、HTTP 小工具 |

### 3. API 接口（server/travel-api.ts 装配）

| 接口 | 方法 | 说明 | 依赖模块 |
|------|------|------|----------|
| `/api/config` | GET | 返回 `{ hasKey, model }` | `resolveLlmConfig` |
| `/api/chat` | POST | 组装 SYSTEM_PROMPT + 消息，转发 LLM，返回 `{ content }` | `chatCompletion` |
| `/api/fetch-url` | POST | 抓取网页正文（截断 3000 字），登录墙降级 | `fetchPageText` |
| `/api/plan-map` | GET | 方案地图：地理编码 + 高德静态图，代理返回 PNG（无 Key / 定位失败静默隐藏） | `fetchPlanMap` |

**server/lib 拆分**：

| 模块 | 导出 | 说明 |
|------|------|------|
| `llm.ts` | `resolveLlmConfig` / `chatCompletion` | 环境变量解析（兼容 `MOONSHOT_*` 旧名）与 OpenAI 兼容调用 |
| `url-fetch.ts` | `extractText` / `fetchPageText` | HTML 正文提取与带超时/UA 的抓取 |
| `amap.ts` | `fetchPlanMap` | 高德地理编码 + 静态图点位标注拼装，返回 PNG 字节 |
| `http.ts` | `readBody` / `sendJson` | 请求体读取与 JSON 响应 |

### 4. 前端数据流与 JSON 协议

**核心协议**（`src/types/index.ts`）：

模型每次回复都是一个 JSON 对象 `ModelReply`：

```ts
{
  reply: string,              // 本轮要说的话（支持 Markdown）
  options?: string[],         // 快捷选项（可组合）
  round?: number,             // 0~3 轮次
  profile?: Record<string, string>,  // 增量画像
  ready_for_plan?: boolean,   // 是否可生成方案
  plan?: string,              // 生成/迭代模式：完整方案
  plan_version?: number,      // 方案版本号
  map_points?: { name, city, day, seq }[],  // 方案地图点位（生成/迭代时输出，≤10 个）
}
```

- `parseModelReply`（`lib/api.ts`）稳健解析：容忍 ` ```json ` 包裹或前后杂文本；解析失败时把原文当 `reply` 展示
- `PROFILE_KEYS`：23 个固定键，模型只能从该集合取键，避免同义词键名发散
- `ROUND_LABELS`：`['快速定位', '骨架信息', '动机与偏好', '细节与底线']`

**页面职责**（`pages/Home.tsx`）：

| 状态 | 说明 |
|------|------|
| `turns` | 对话记录（`ChatTurn[]`） |
| `profile` | 增量累积的需求画像，实时渲染到右侧面板 |
| `round` | 当前轮次，驱动顶部进度条 |
| `plan` / `planVersion` | 已生成方案及其版本 |
| `readyForPlan` | 高亮「生成方案」按钮 |

**方案生成与迭代**：点击「生成方案」发送 `[GENERATE_PLAN]` → 模型返回 `plan` + `plan_version: 1`；之后每轮反馈都返回修订版完整方案，`plan_version` 逐次 +1；客户满意时不再返回 `plan`，改为收尾 + 行前提醒。

### 5. 需求画像面板（components/travel/ProfilePanel.tsx）

- 遍历 `PROFILE_KEYS` 渲染已收集项，额外键追加在末尾
- `餐饮要求` / `雷区底线` / `证件健康` 三项标记 ⚠（硬约束）
- 空态提示「随着对话进行，你的需求会实时汇总在这里」

### 6. 如何启动

**前置条件**：已安装 Node.js 20+（含 npm）。

**步骤**：

1. 进入应用目录：

   ```bash
   cd travel-planner-app
   ```

2. 安装依赖（首次运行）：

   ```bash
   npm install
   ```

   > 在受限/沙箱环境下若 npm 缓存目录不可写，可改用 `npm install --cache .npm-cache` 把缓存重定向到项目内（`start.ps1` 已内置该处理）。

3. 配置 LLM（`.env`）：

   ```bash
   cp .env.example .env
   ```

   编辑 `.env`，至少填入 `LLM_API_KEY`；如需指定服务商/模型，一并填 `LLM_BASE_URL` 与 `LLM_MODEL`。未配置 Key 时页面可打开，但 AI 对话不可用。

4. 启动开发服务器：

   ```bash
   npm run dev
   ```

   或在 PowerShell 中用一键脚本（自动装依赖 → 检查 `.env` → 启动）：

   ```powershell
   .\start.ps1
   ```

5. 打开浏览器访问：http://localhost:3000/

**验证**：启动后访问 `http://localhost:3000/api/config`，返回 `{"hasKey":true,"model":"..."}` 即表示 Key 已就绪、可正常对话。

**其他脚本**：

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器（Vite + API 中间件，端口 3000） |
| `npm run build` | 类型检查 + 生产构建 |
| `npm run preview` | 预览生产构建产物 |
| `npm run lint` | ESLint 检查 |

**相关配置**：`vite.config.ts` 固定端口 3000、`@ → ./src` 别名，并注册 `travelApiPlugin(env)`（用 `loadEnv` 加载 `.env`）；`.env` 已 gitignore，`.env.example` 为模板。

---

## 核心理念

1. **LLM 做判断，代码做执行** — LLM 只负责对话与方案产出；链接抓取、JSON 解析、状态管理均为确定性代码
2. **JSON 交接** — 模型与前端用严格 `ModelReply` 协议传递，不做自然语言协商
3. **分轮迭代，不像审讯** — 每轮 3~5 问、选项优先、后轮消化前轮、够用即停
4. **可信度优先** — 不编造精确数字、不推荐具体店铺（改给「平台 + 关键词」指引）、时效性信息标注「建议出行前核实」
5. **渐进式披露** — 大段 Prompt 与可复用 helper 从入口文件拆出；skill 也按 SKILL.md / references 拆职责

---

## 本期边界

- 实现：分轮需求收集 → 实时画像 → 方案生成与多轮迭代（单用户、单会话）
- 不做：账号体系、会话持久化、多端同步、平台直发（方案落地为文本而非下单）
- 时效性信息（价格/开放时间/签证）由模型标注「建议出行前核实」，不落库
