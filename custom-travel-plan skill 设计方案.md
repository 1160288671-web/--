# custom-travel-plan Skill 设计方案

> 本文档是 `custom-travel-plan` skill 的设计说明：设计目标、架构决策、流程设计与维护方式。
> 讨论过程与原始思路见《定制化旅行方案讨论记录v1.md》。

---

## 1. 设计目标

让 agent 面对"帮我做个旅行方案"这类请求时，能像专业旅行定制师一样工作：

1. **不缺关键信息**——骨架信息、动机、底线、硬约束都有明确的收集位
2. **不像审讯**——分轮提问，每轮 3~5 个，选项优先
3. **不返工**——雷区、证件、预算冲突在出方案前暴露，而不是出方案后
4. **不照字面执行**——客户的模糊表述（"想看海"）经过翻译和确认再落地

## 2. 文件结构

```
custom-travel-plan/
├── SKILL.md                        # 主流程：原则、分轮工作流、三层分析法、产出要求
└── references/
    └── question-bank.md            # 问题库：各轮问题模板、选项设计、条件分支、话术
```

**为什么这样拆**（渐进式披露）：

- SKILL.md 保持精简（约 100 行），只装"流程怎么走"，每次触发都会加载，必须控制上下文成本
- 问题库体量大、细节多，且只在实际提问时需要，放到 references 按需加载
- 两份文件职责互斥：流程逻辑只在 SKILL.md，具体问题措辞只在 question-bank.md，避免双处维护

**安装位置**：`C:\Users\11602\AppData\Roaming\kimi-desktop\daimon-share\daimon\skills\custom-travel-plan\`（受管 skills 根目录，触发时自动发现）。工作区保留打包副本 `custom-travel-plan.skill`。

## 3. 触发设计

frontmatter 的 description 覆盖两类触发场景：

- **终端用户场景**："帮我做旅行方案""规划行程""想去哪玩"（含蜜月/亲子/度假等具体词）
- **从业者场景**：帮第三方客户做旅行定制

description 同时声明了核心方法论（分轮迭代、三层分析），让 agent 在加载正文前就知道这个 skill 的工作方式。

## 4. 核心流程设计

### 4.1 分轮迭代结构（核心设计决策）

```
Round 0  快速定位    → 判断客户类型，决定走几轮、从哪轮进
Round 1  骨架信息    → 出发地 / 时间 / 预算 / 同行人 / 目的地意向
Round 2  动机与偏好  → 旅行动机 / 参照样本 / 玩法倾向 / 出片意愿
Round 3  细节与底线  → 吃 / 住 / 行 / 节奏 / 雷区 / 证件健康
Round N  按需迭代    → 补缺口、解矛盾；上限 4 轮纯提问
```

**关键规则（写死在 SKILL.md 核心原则里）：**

| 规则 | 理由 |
|---|---|
| 每轮 3~5 个问题 | 超过就像审讯，客户流失 |
| 后轮必须消化前轮答案 | 引用它、收窄它、追问它；禁止矛盾或重复提问（如已知带 3 岁孩子则不再推徒步选项） |
| 选项式优先 | 很多偏好是被问到才意识到的，选项帮客户表达 |
| 每轮开头复述确认 | 防止在错误前提上越问越深 |
| 够用即停 | 信息够出方案就停；客户不耐烦时给方案并标注假设 |
| 最多 4 轮纯提问 | 之后必须出初版方案，用方案引出新反馈，避免无限收集 |

### 4.2 Round 0 的分流逻辑

不是所有客户都需要完整流程，按初始状态分流：

- **目标明确型**（"我要去云南帮我排行程"）→ 跳过目的地探索，直接进 Round 2
- **模糊意向型**（"想看海但不知道去哪"）→ 走完整流程，Round 2 做目的地翻译
- **纯探索型**（"最近想出去走走"）→ 先问动机和参照样本，再谈目的地

### 4.3 三层分析法（信息处理顺序固定）

```
硬约束 → 可行域      （预算/日期/证件/健康/雷区，做减法）
   ↓
需求翻译 → 真实诉求   （"想看海"= 放松?玩水?拍照? 用假设反问确认）
   ↓
软偏好加权 → 排序取舍 （冲突时把取舍摆给客户选，不替他决定）
```

顺序不可颠倒：先在可行域内翻译需求才有意义，取舍必须发生在可行解之间。

### 4.4 产出要求

方案必须包含的要素在 SKILL.md 中列为检查清单：每日行程、交通（含理由）、住宿（匹配床型/风格/位置）、餐饮（避开所有忌口）、预算分解（呼应预算结构偏好）、社交记忆点（动机含展示时 1~2 个）、风险提示（签证/天气/旺季/预订时效）、至少 1 个替代方案。

另有一条独立规则：**时效性事实（价格、开放时间、签证政策、航班）必须搜索核实，禁止凭记忆写具体数字。**

## 5. 问题库设计

question-bank.md 按轮次组织，设计上注意了三点：

1. **⚠ 硬约束标记**：忌口、床型、雷区、证件等必问项单独标记，对应场景出现时不可省略
2. **条件分支表**：蜜月 / 带 3~6 岁孩子 / 带 7~14 岁孩子 / 带老人 / 朋友多人 / 独行 / 商务顺道，各有追加问题，避免主流程臃肿
3. **矛盾处理话术**：预算冲突、照骗景点校准、日期弹性三类高频矛盾各给了一个示例话术，把取舍显式摆给客户

提问时的元规则：**从库中选取并根据已收集答案改写，不照抄。**

## 6. 关键设计取舍记录

| 取舍 | 决策 | 理由 |
|---|---|---|
| 一次性问全 vs 分轮 | 分轮 | 一次性 20 问像审讯；分轮能根据答案收窄后续问题 |
| 开放式提问 vs 选项式 | 选项优先，开放补充 | 客户偏好需要被引导表达 |
| 社交属性放哪 | 归入 Round 2 动机层 | 它是动机的一个分支（放松/体验/陪伴/社交展示），不是独立维度 |
| 雷区何时问 | Round 3 而非 Round 1 | 客户需要先对方案有具象感才说得出"绝对不要什么"；但必须在出方案前 |
| 固定 3 轮 vs 动态轮次 | 动态，上限 4 轮 | 目标明确型客户不该被迫走全流程 |
| 问题内嵌 vs 独立文件 | 独立 references 文件 | 控制 SKILL.md 上下文成本，问题库可独立扩充 |

## 7. 维护与迭代

- **改流程**（轮次结构、分析顺序、产出要求）→ 改 SKILL.md
- **改问题**（新增场景分支、优化措辞、补充话术）→ 改 references/question-bank.md
- **改触发**（发现该触发没触发/误触发）→ 改 frontmatter 的 description
- 讨论层面的新想法先记入《定制化旅行方案讨论记录》系列文档，沉淀后再同步进 skill，保持"讨论稿 → 设计文档 → skill 实现"的演进链路
- 修改后重新打包：在 skills 根目录执行 `zip -r custom-travel-plan.skill custom-travel-plan`，副本存工作区
