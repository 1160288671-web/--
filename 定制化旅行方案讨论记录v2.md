# 定制化旅行方案：讨论记录 v2

> 继《定制化旅行方案讨论记录v1.md》之后，记录 Web 应用落地过程中的两轮产品反馈与决策。
> 对应产物：`travel-planner-app/`（Web 应用）、`custom-travel-plan` skill（待同步）。

---

## 一、从 skill 到 Web 应用

讨论框架（v1）封装成了可交互的 Web 应用 `travel-planner-app`：

- React 19 + TypeScript + Vite 单页应用，服务端是 Vite 插件内嵌的 `/api/*` 中间件
- LLM 走任意 OpenAI 兼容接口（`.env` 配置 `LLM_API_KEY` / `LLM_BASE_URL` / `LLM_MODEL`），当前使用 DeepSeek（deepseek-chat），Key 只存本地服务端，不进前端
- 交互协议：模型每轮回复单个 JSON 对象（`reply` / `options` / `round` / `profile` / `ready_for_plan`），前端稳健解析
- 右侧"需求画像"面板随对话实时累积，硬约束项（忌口/雷区/证件健康）标 ⚠

## 二、初版体验反馈（11 条）与对应决策

| # | 反馈 | 决策 |
|---|---|---|
| 1 | 出发时间问得太泛，方案要结合季节/天气 | Round 1 必问具体日期或月份，并向用户解释为什么（"夏天和冬天的海边完全不同"）；方案必含季节/天气设计说明 |
| 2 | 打字交互太多，选项点了就发送 | 快捷选项改为**暂存进输入框**，可连续点选拼接、可编辑后发送 |
| 3 | 问法生硬（"旅行的主要动机是什么？"） | 提示词新增语气规范：贴心朋友式口语、严禁问卷腔，附正反对比例子 |
| 4 | 需求画像不更新 | 双修复：画像面板显示全部键（固定键优先 + 额外键兜底）；`profile` 改为必填、禁止自创键名；启用 `response_format: json_object` 强制结构化输出 |
| 5 | 用户转发链接/推送能否理解 | 新增 `/api/fetch-url` 服务端抓取正文喂给模型；小红书/朋友圈有登录墙，抓不到则请用户描述 |
| 6 | 快捷选项点不动 | 根因是模型偶发不按 JSON 输出导致选项丢失；JSON 模式 + options 必填解决 |
| 7 | 方案缺通勤体感信息 | 方案必含关键通勤的方式/耗时/费用（如"厦门北站→市区：地铁约50分钟5元"） |
| 8 | 不推荐店铺时给搜索指引 | 给"平台+关键词"指引（"大众点评搜'厦门大学思明校区 姜母鸭'"），不编店名 |
| 9 | 缺项目时长与体力评估 | 每个项目标注【预计时长】【体力消耗：低/中/高】 |
| 10 | 餐饮与行程脱节 | 餐饮安排与当日行程地理绑定，顺路吃、不绕回头路 |
| 11 | 玩与吃的主次 | Round 2 新增必问"这趟更在意玩得好还是吃得好"，决定方案以游玩线还是美食线为导向 |

## 三、增量功能：主动提议标志性体验

- 洞察：用户对目的地一开始没概念（"不知道成都能看熊猫"），想法随讨论逐渐清晰
- 决策：目的地明确后（通常 Round 2），模型主动给出 4~6 个该地标志性体验选项；用户后来冒出的新兴趣点记入画像「必体验清单」，方案中优先安排进核心时段
- 这让用户感到"代理在替我着想"

## 四、产品哲学：善意、可靠性、可信性

贯穿所有回复的原则（已写入系统提示词）：

- **不知道就明说不知道**，不确定就标注"建议核实"——宁可少说，绝不编造；坦白不确定的地方，用户才会相信其余信息
- **不推荐具体店铺**（排队/品控/歇业风险太大），改为搜索指引
- **数字只给量级**（"大约50分钟""人均80左右"），不伪造精确值

## 五、方案迭代闭环

方案很少一轮到位，决策如下：

1. 生成方案时先给"方案速览"（4~6 条关键决策：路线主线、住宿档位、预算分配、必体验落位、主要取舍），引导用户看详细方案并提反馈
2. 方案后的每句话视为修改意见：返回**完整修订版**（非 diff）+ 本次调整说明，版本号 +1
3. 只改用户提的点，不借机大改；不催促定稿
4. 用户说"可以了"则收尾，附行前提醒清单（证件签证、需提前预订项、待核实时效信息、季节衣物）

## 六、地图接入方案（已设计，待实施）

### 选型

| 方案 | Key | 国内适配 | 结论 |
|---|---|---|---|
| **高德 Web 服务** | 免费个人 Key | 最好 | ★ 选用：静态图 API 直接出 PNG，契合"方案页出现一次、不实时刷新" |
| 百度地图 | 免费 Key | 好 | 备选 |
| Google Maps | 付费+网络受限 | 差 | 排除 |
| OpenStreetMap/Leaflet | 免 Key | 国内偏移、POI 少 | 留作海外目的地兜底（后续） |

### 数据流

```
模型出方案（JSON 新增 map_points: [{name, city, day, seq}]）
  → 前端请求 GET /api/plan-map?points=[...]
  → 服务端：高德地理编码拿坐标 → 拼静态图 URL → 代理返回 PNG
  → 方案卡片顶部显示地图 + 图例
```

### 动线绘制：两种画法的取舍

- **A. 动线示意线（v1 采用）**：按天连线、每天一色；直线示意而非真实路网，图上标注"动线示意图（非实际道路）"。简单稳定，满足"判断动线是否合理、是否来回折返"的需求
- **B. 真实导航路径（留作升级）**：调路径规划 API 画真实道路。调用量大、失败点多、URL 超长需抽稀；且出行时用户会用导航 App，这张图不承担导航职责

### 关键设计决策

1. Key 只在服务端（静态图 URL 含 Key，由服务端代理图片字节，前端不可见）
2. 随方案版本更新：修订时 `map_points` 一并更新、重新取图
3. 无 Key / 定位失败时地图模块静默隐藏——地图是辅助，不硬撑（可信度原则）
4. 定位精度靠提示词约束（标准地名 + 必填城市）+ 取地理编码首条结果
5. 海外目的地第一版不兜底，自动隐藏

---

## 待办

- [ ] 用户注册高德 Web 服务 Key → 实施地图接入（方案见第六节）
- [ ] 把两轮改进同步进 `custom-travel-plan` skill 本体，保持 skill / app / 文档三者一致
- [ ] 真实使用观察：DeepSeek 对"不重复提问""语气规范"的遵循度
