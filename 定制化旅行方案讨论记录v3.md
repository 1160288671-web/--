# 定制化旅行方案：讨论记录 v3

> 继《定制化旅行方案讨论记录v2.md》之后，记录「方案页地图缺失时如何兜底」的讨论与决策。
> 对应产物：`travel-planner-app/`（Web 应用）的方案渲染层（`server/lib/render-plan.ts`、`server/lib/amap.ts`）。

---

## 一、问题背景

地图接入已落地（见 v2 第六节），但实际测试暴露一个兜底盲区：

- 大型景区 / 小众地名（如「乌兰布统草原」）不是标准行政区，高德地理编码定位不到，`fetchCityMap` 也兜不住。
- 此时方案页渲染出一句「此页暂未生成路线地图，请以文字行程为准」，观感不佳。

核心诉求：**地图给不了时，用一张「效果图 / 景点照」替代，而不是退化成难看的占位符。**

## 二、分层降级策略（决策）

按优先级依次尝试，逐层兜底：

1. **有地图 → 照常显示地图**（现状，不动）。
2. **地图失败 → 拉取图库真实照片**：
   - 封面页用「目的地 / 体验」搜索；
   - 每日页用「当天核心地点 / 活动」搜索。
3. **图库也失败 → 更美观的纯 CSS 占位插画**：复古波普风几何装饰 + 目的地大字，替代现有那句干巴巴的提示。

图片不再以 `data:image/png;base64` 内嵌，改为外链 URL，顺带解决 HTML 体积膨胀问题；服务端统一代理 + 缓存，避免重复渲染重复请求。

## 三、照片搜索的核心思路：地名 vs 体验分流

搜索关键词是关键，不能「无脑搜地名」——小众地名直接命中率低，搜出来可能是广告图或无关图。

| 场景 | 搜索策略 | 示例 |
|---|---|---|
| 大众地名 / 城市地名 | **直接搜地名** | 成都、重庆（实测效果不错） |
| 小众地名 / 大型景区 | **按体验 / 活动搜** | 乌兰布统 → 搜「草原」「骑马」（实测效果不错） |

依据：小众地名在图库里直接命中的概率低，但用户真正想看的是那个地方「能玩什么、有什么体验」；把地名翻译成体验关键词（草原、骑马、日出、沙漠、古城…），命中率和相关性都明显更好。

落地时由 `map_points`（地名）+ 当天行程标题（活动）共同提供搜索候选，优先用可识别的大众地名，否则降级到体验词。

## 四、图源选型（避开广告图）

要求：**真实摄影、无广告、可商用**，排除搜索引擎图片（版权混杂、易命中广告图）。

| 图源 | 定位 | Key | 说明 |
|---|---|---|---|
| Unsplash | 主力 | 需注册（免费额度） | 摄影质量最高，英文搜索为主 |
| Pexels | 主力 / 备胎 | 需注册（免费额度） | 质量接近 Unsplash，额度更宽 |
| Wikimedia Commons | 兜底 | 无需 Key | 支持中文搜索，国内小众景点命中率更高；质量参差、需注意 CC 署名 |

组合建议：Unsplash/Pexels 做主力（质量优先）+ Wikimedia 做国内小众景点兜底。

## 五、后续：图片美化 skill

搜索到的原图后续经一个独立 skill 统一「美化」（构图 / 风格 / 尺寸适配），与图源选择解耦——先定图源，再叠加美化。

## 六、待办

- [ ] 用户实测 Unsplash / Pexels 搜「乌兰布统草原」等关键词效果，确认选哪家注册 Key
- [ ] 实施分层降级：`amap.ts` 失败 → 图库拉图 → 美化占位符
- [ ] 服务端图库代理 + 缓存（避免重复渲染重复请求、限流）
- [ ] 落地「地名 vs 体验」关键词分流逻辑
- [ ] 新增「图片美化」skill（构图 / 风格统一），并同步进 `custom-travel-plan` skill 本体
